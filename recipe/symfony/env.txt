IMGPROXY_HOST=https://imgproxy.example.com
IMGPROXY_KEY=
IMGPROXY_SALT=

###> imgproxy v4 PRO server-side options ###
# The variables below are read by the imgproxy *server* (container), NOT by
# Symfony. Set them in the imgproxy service's environment. See README.

# Persistent internal cache — store processed images so you no longer need a
# caching reverse proxy in front of imgproxy. Adapter: fs | s3 | gcs | abs | swift
# IMGPROXY_CACHE_USE=s3
# IMGPROXY_CACHE_BUCKET=my-imgproxy-cache
# IMGPROXY_CACHE_PATH_PREFIX=cache
# IMGPROXY_CACHE_S3_REGION=us-east-1
# IMGPROXY_CACHE_S3_ENDPOINT=
# Filesystem adapter instead of S3:
# IMGPROXY_CACHE_FS_ROOT=/var/cache/imgproxy

# Fetch source images directly from S3 (use s3://bucket/key source URLs):
# IMGPROXY_USE_S3=true
# IMGPROXY_S3_REGION=us-east-1
# IMGPROXY_S3_ENDPOINT=
# IMGPROXY_S3_ALLOWED_BUCKETS=

# Allow info-endpoint options (PRO). Leave blank to allow all, or restrict:
# IMGPROXY_ALLOWED_INFO_OPTIONS=size,format,dimensions,classify_objects,detect_objects,blurhash
###< imgproxy v4 PRO server-side options ###
